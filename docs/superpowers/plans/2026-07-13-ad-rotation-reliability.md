# 轮播广告规则与可靠派发实现计划

1. 固化置顶池/排除池选择与群归属校验，补齐管理入口。
2. 扩展历史表为发送前创建的唯一派发 occurrence，保存完整内容和规则快照。
3. 拆分 planner/store、Telegram executor 和 worker，增加租约、重试、不确定态与健康失败上浮。
4. 仅在成功事务中推进规则游标和广告发送统计。
5. 增加历史筛选、失败重试、取消和不确定态二次确认重放。
6. 补齐 fresh SQL、迁移、启动兼容、Schema Gate 与全量回归。
